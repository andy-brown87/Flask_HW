<html>
    <head>
        <title>{{title}}</title>
    </head>
    <body>
        <h1>{{title}}</h1>
        <ul> 
            {% for order in orders %}
            <li>{{ order.input_customer_name }} <b>{{ order.input_order_date }} </b> </li>
            {% endfor %}
        </ul>
    </body>


</html>